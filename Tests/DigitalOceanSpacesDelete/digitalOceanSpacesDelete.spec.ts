import tl from '@DOSDelete/utils/tl'
import { Delete } from '@DOSDelete/utils/Delete.ts'
import { Parameters } from '@DOSDelete/utils/Parameters.ts'

describe('Inital Delete task', () => {
  test('should call, start and wait for result correctly', () => {
    // TODO: learn how to create these tests, maybe using rewire
    expect('').toEqual('')
  })
})
